import pyodbc

# conn=pyodbc.connect(r'DRIVER={Microsoft Access Driver (*.mdb, *.accdb)};DBQ=./Database4.accdb;')
# cursor=conn.cursor()
# cursor.execute("INSERT INTO Teacher VALUES(?,?)",(5,"Hussain"))
# conn.commit()
# cursor.execute("UPDATE Teacher SET tname=?",("Taimoor"))
# conn.commit()
# cursor.execute("SELECT * FROM Teacher")
# row=cursor.fetchall()
# print(row)
# conn.close()